name: PR Preview Comment

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  comment:
    runs-on: ubuntu-latest

    env:
      CLEAN_BRANCH: ${{ github.head_ref.replace('/', '-') }}

    steps:
      - name: Create PR Comment
        uses: thollander/actions-comment-pull-request@v2
        with:
          message: |
            🔍 **Preview is live!**

            👉 https://othaldo.github.io/stevenschwarz/${{ env.CLEAN_BRANCH }}/
